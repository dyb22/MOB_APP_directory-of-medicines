<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Mobile" parent="Base.Theme.Mobile" />

    <!-- Прозрачный индикатор выбранного пункта (фон рисуем через itemBackground) -->
    <style name="Widget.Mobile.BottomNavigationView.ActiveIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@android:color/transparent</item>
    </style>

    <style name="Widget.Mobile.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="itemActiveIndicatorStyle">@style/Widget.Mobile.BottomNavigationView.ActiveIndicator</item>
        <item name="itemRippleColor">@android:color/transparent</item>
    </style>

    <style name="BookmarksToolbarTitle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">15sp</item>
    </style>
</resources>